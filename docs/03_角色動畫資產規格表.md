# 03 — 角色動畫資產規格表

> **這份文件是規劃期的規格表，四個角色都做完了。**
>
> **權威來源已經移到 `specs/`：**
> 角色特徵看 `specs/characters/<id>.json`、調色盤看 `specs/palettes/<id>.json`、
> 動畫定義看 `specs/animations/<id>.anim.json`、提示詞看 `specs/anim_prompt/`。
> 那些檔案是程式真的在讀的，本文件不是。
>
> 本文件保留下來的價值是**規劃期的推理**：比例為什麼這樣訂、
> 吉娃娃為什麼刻意放大、影格預算為什麼砍到 86。實際數字以 `specs/` 為準。
>
> 第五節（部件階層）與第七節的動畫清單描述的是**已移除的部件旋轉路線**，
> 只作為歷史記錄，不要照著做。

> ### 修訂 R1 — 改為真像素藝術風格
>
> 美術方向從「平塗插畫」改為**真像素藝術**。這個改變同時解掉了兩個包袱：
>
> 1. 原本要三令五申「不准用漸層」的規則，變成像素風的自然結果——
>    限色是像素藝術的本質，和 4bpp 索引色的硬體限制是同一件事
> 2. 真像素角色本身就小，**四角色同框只要 254 px（< 320）**，
>    原本規劃的兩套縮放比例（`solo` / `group`）整個不需要了
>
> 受影響的章節（一、二、五）已更新。**生成端的實測規則與工具用法見 `docs/05_資產管線.md`。**

---

> ### ⚠️ 已淘汰的路線（2026-08-01 移除）
>
> 本文件部分內容描述「部件切分 → 擺姿勢 → 旋轉 → 合成」的路線。
> **該路線已刪除**（`assemble.py` / `cutparts.py` / `specs/poses/` / `art/rigs/`）。
>
> 失敗原因：整條腿繞肩關節旋轉會讓腳掌畫弧離開地面——真實的腿在膝肘彎曲、
> 腳掌貼地推進，單段式部件做不到。實測 `walk` 與 `idle_breathe` 產出完全相同的影格。
>
> **現行路線**：AI 一次生成一張含全部影格的圖 → `cutstrip.py` 切開對齊
> → `pixelate.py` → `bake.py`。見 `docs/05_資產管線.md` 第三節。


## 一、全域母版規格

| 項目 | 規格 |
|---|---|
| 生成尺寸 | 向 gpt-image-2 要 **1024 × 1024**（實際輸出會不同，見下） |
| 生成背景 | **純洋紅 `#FF00FF`**。gpt-image-2 不支援透明背景 |
| 最終 sprite | **64 × 64 格**（狗實測填滿約 64 × 53） |
| 格式 | PNG，RGBA，1-bit alpha |
| 視角 | 3/4 側面，視平線在角色胸高 |
| 描邊 | 深色統一描邊，最終 sprite 上**恰好 1 px** |
| 色彩 | 每角色 **≤ 16 色**（含透明 index 0） |
| 陰影 | 接地陰影為**獨立部件**，不畫在角色身上 |

> ⚠️ **不要依賴指定的生成尺寸。** 實測要 1024×1024 得到 1341×1173，
> 要 1536×1024 得到 1774×887。`tools/pixelate.py` 會裁到內容範圍再縮放，
> 所以輸出尺寸不一致沒有影響。

### 風格硬性規則

裝置端貼圖是 16 色索引 + 1-bit alpha。因此必須：

✅ 平塗，每個材質最多 3 階（暗 / 中 / 亮）
✅ 清晰封閉的深色描邊，最終 sprite 上 1 px
✅ 色塊邊界銳利，大片單色區域

❌ 漸層 / 噴槍柔邊 / 半透明 / 發光 / 模糊
❌ 寫實毛髮渲染、逐根毛、照片材質

**這不是美感偏好，是硬體限制。** 有漸層的圖量化到 16 色會出現嚴重色帶，
RLE 壓縮率也會從 2.5× 掉到接近 1×。

完整提示詞風格區塊見 `specs/style_lock.md`，**一字不改地貼進每次生成**。

> **最重要的一條**：提示詞只寫 "pixel art" 不夠。實測模型會理解成
> 「畫高細節圖再套像素化濾鏡」，等效解析度會到 150×160，細節全糊在一起。
> 必須明確寫出目標像素尺寸並強調 "genuinely low-resolution"。

---

## 二、四角色比例鎖定表

以 512 母版座標系，站立姿態測量。**這張表一旦定案就不再改動。**

| 角色 | 全高(px) | 佔公主比 | 體長(px) | 肩高(px) | 頭身比 | 眼睛佔頭寬 |
|---|---:|---:|---:|---:|---|---:|
| `ice_princess` | **420** | 100% | — | — | 4.5 頭身 | 20% |
| `chihuahua` | **145** | 34.5% | 165 | 105 | 3 頭身 | 22% |
| `brown_mixed` | **250** | 59.5% | 300 | 195 | 3.5 頭身 | 15% |
| `brindle_guard` | **265** | 63.1% | 310 | 210 | 4 頭身 | 16% |

### 關於吉娃娃的刻意放大

真實比例下，吉娃娃相對於一個 4 歲女孩約為 **28%**。本表用 **34.5%**，
刻意放大約 6 個百分點。理由：在 `group` 縮放下（70%），
28% 會讓吉娃娃只有 **32 px 高**，臉部特徵完全消失，小孩認不出那是誰家的狗。
34.5% 換算為 40 px，剛好保住耳朵形狀與額頭白線的辨識度。

這是**唯一一處**允許偏離真實比例的地方，其餘一律照實。

### 輸出尺寸（單一比例，不需要兩套縮放）

改成真像素藝術之後，角色本身就小，四角色同框放得進 320 px，
所以**不需要 `solo` / `group` 兩套縮放**。這是改用像素風的直接收益。

規劃值與**四個角色做完之後的實測值**：

| 角色 | 規劃 高×寬 | 實測 寬×高 | 面積 | 影格格 |
|---|---|---|---:|---|
| `ice_princess` | 105 × 60 | **38 × 90** | 2298 | 64 × 96 |
| `chihuahua` | 36 × 41 | **44 × 36** | 986 | 64 × 56 |
| `brown_mixed` | 62 × 75 | **64 × 54** | 1809 | 64 × 56 |
| `brindle_guard` | 66 × 78 | **50 × 55** | 1253 | 64 × 56 |
| | | **同框寬 196** | | **< 320** ✓ |

影格格**不是統一的**，由 `specs/characters/<id>.json` 的 `render.sprite_cell` 決定，
`tools/cell.py` 是唯一的讀取點。狗是 64×56、公主是 64×96
（規劃寫 64×112，那是在狗的格還是 64×64 的年代訂的，等比例換算到 56 之後是 96）。
空白區域由 RLE 壓到接近零成本——四角色 332 格合計 235 KB。

> **真像素藝術不可以在裝置端縮放。** 任意比例縮放會破壞像素網格，
> 看起來會很糟。所有尺寸在 build time 就決定好。

---

## 三、角色身份鎖定

> **本節的 `TBD_FROM_PHOTO` 欄位全部填完了，但不是填在這裡。**
> 四個角色的辨識特徵在 `specs/characters/<id>.json` 的 `identity_features`，
> 那是程式（`validate.py`）真的在讀的。本節保留作為當初要問的問題清單。

以下 `identity_features` 是**不可協商的辨識特徵**。
每一張生成圖都要逐項檢查，缺任一項即退回重生。

### 3.1 `chihuahua` — 棕白長毛吉娃娃

| 項目 | 規格 | 狀態 |
|---|---|---|
| 體型 | 小型、身體纖細，胸腔窄，腰身明顯 | ✅ |
| 毛色 | 三色：棕（主）／白（胸腹、嘴周）／淺褐（過渡） | ✅ |
| **額頭白線** | 從鼻梁延伸至額頭，寬度約頭寬 1/5 | ✅ |
| ↳ **偏向** | 白線偏向哪一側、偏移量 | ⚠️ `TBD_FROM_PHOTO` |
| 胸口 | 白色，範圍與形狀 | ⚠️ `TBD_FROM_PHOTO` |
| 嘴周 | 白色 | ✅ |
| 眼睛 | 大而圓，深色，佔頭寬 22% | ✅ |
| 耳朵 | 大、尖、完全豎立，耳緣有長飾毛 | ✅ |
| 尾巴 | 蓬鬆長毛，通常上揚捲向背部 | ✅ |
| 四肢 | 細長，關節明顯 | ✅ |
| 個性 | 黏人、警覺、活潑；動作快而碎 | ✅ |
| 項圈顏色 | 作為角色識別色 | ⚠️ `TBD_FROM_PHOTO` |

**鏡射政策**：`head`、`muzzle` 因額頭白線不對稱 → `mirror_safe: false`，左右各畫。
其餘部件可鏡射。

---

### 3.2 `brown_mixed` — 棕色憨厚米克斯

| 項目 | 規格 | 狀態 |
|---|---|---|
| 體型 | 中大型，身形厚實略胖，胸深，腰身不明顯 | ✅ |
| 毛色 | 巧克力棕，單色短毛 | ✅ |
| ↳ 深淺 | 具體色階與是否有較淺的腹部 | ⚠️ `TBD_FROM_PHOTO` |
| 頭部 | 寬，顱頂圓 | ✅ |
| 鼻口 | 厚實、方，不尖 | ✅ |
| 耳朵 | 自然下垂，耳尖朝前下方 | ✅ |
| 眼睛 | 溫和，眼角略下垂，佔頭寬 15% | ✅ |
| 坐姿 | 有明顯重量感，臀部落地扎實，身體略後傾 | ✅ |
| 四肢 | 粗壯 | ✅ |
| 個性 | 慢熟、愛吃、溫和；動作慢而穩 | ✅ |
| 白斑 | 是否有胸口 / 腳掌白斑 | ⚠️ `TBD_FROM_PHOTO` |
| 項圈顏色 | 角色識別色 | ⚠️ `TBD_FROM_PHOTO` |

**鏡射政策**：單色無不對稱斑紋 → 全部件 `mirror_safe: true`。省下一半工。

---

### 3.3 `brindle_guard` — 虎斑護衛犬米克斯

| 項目 | 規格 | 狀態 |
|---|---|---|
| 體型 | 中大型，結實，胸口寬，肌肉線條明確 | ✅ |
| 毛色 | 棕黑虎斑（brindle），深色縱向條紋在棕底上 | ✅ |
| ↳ 紋路密度與走向 | 條紋粗細、間距、分佈區域 | ⚠️ `TBD_FROM_PHOTO` |
| ↳ 臉部斑紋 | 是否有面具狀深色、眉點 | ⚠️ `TBD_FROM_PHOTO` |
| 頭部 | 偏獵犬／護衛犬臉型，吻部較長 | ✅ |
| 耳朵 | 尖、豎立 | ✅ |
| 眼睛 | 專注，眼神有方向性，佔頭寬 16% | ✅ |
| 站姿 | 警戒，重心略前，尾巴水平 | ✅ |
| 個性 | 聰明、忠誠、喜歡巡邏 | ✅ |
| 項圈顏色 | 角色識別色 | ⚠️ `TBD_FROM_PHOTO` |

**⚠️ 表現底線**：不露牙、不皺鼻、不做撲咬姿態、不畫豎起的背毛。
守護感來自**站姿的穩定與視線的專注**，不是威嚇。這是給 3 歲小孩的產品。

**鏡射政策**：虎斑紋路全身不對稱 → **所有部件 `mirror_safe: false`**，左右各畫全套。
這是三隻狗裡工作量最大的一隻，排程時要留時間。

---

### 3.4 `ice_princess` — 冰雪公主

> **⚠️ 本節的設計已經作廢。** 權威來源是 `specs/characters/ice_princess.json`。

原始設計是「及地冰藍禮服 + 單側白金長辮 + 半透明披肩 + 小皇冠」。
**那個設計生不出來**——影像模型的輸出審核連續四次拒絕
（`moderation_blocked` / `category: other` / `stage: output`）。

診斷用的是變因控制：只換頭髮→仍被擋；拿掉全部身體輪廓語彙→仍被擋；
改成成人比例→仍被擋；**把冰雪主題整組換成森林→一次通過**。
所以擋的是那一整組指向某商業角色的組合，不是兒童判定也不只是頭髮。

現行設計在剪影層就走開：

| 項目 | 規格 |
|---|---|
| 頭身比 | 4 頭身（幼兒向的可愛比例） |
| 髮色 | 銀灰（近白、冷調、飽和度 7%——刻意不做藍銀，否則會和斗篷糊在一起） |
| 髮型 | 頭頂**左右兩個圓丸子頭**，臉側垂幾綹短髮 |
| 髮飾 | 其中一個丸子頭上別一個淡藍雪花 |
| 眼睛 | 大、藍色、佔頭寬 20%，帶白色高光 |
| 膚色 | 很淺的暖膚色 |
| 基本服裝 | 冰藍色**及膝連帽斗篷**（敞開、帽子放下掛在肩後）+ 近白短洋裝 + 深藍靴子 |
| 配件 | **莓紅圍巾與手套**（全身唯一的暖色） |
| 個性 | 溫柔、照顧者的姿態 |

**IP 注意**：規則原本說「日後對外發表前必須改成原創設計」——
那件事已經被工具強制提前做掉了。程式碼、檔名、提示詞中仍然不得出現
任何商業作品的角色名稱或專有名詞（`specs/anim_prompt/ice_princess.json`
的 `_design_note` 記了完整診斷，**不要把設計改回去**）。

---

## 四、調色盤規格

每角色一組 **16 色**（index 0 固定為透明）。定義在 `specs/palettes/<char>.json`。

以吉娃娃為例的配置邏輯：

| idx | 用途 | idx | 用途 |
|---:|---|---:|---|
| 0 | 透明 | 8 | 淺褐 中 |
| 1 | 描邊（深棕黑） | 9 | 鼻／肉墊 深 |
| 2 | 棕毛 暗 | 10 | 眼 深色 |
| 3 | 棕毛 中 | 11 | 眼 高光（白） |
| 4 | 棕毛 亮 | 12 | 舌／口內 粉 |
| 5 | 白毛 暗（灰藍調） | 13 | 耳內 粉 |
| 6 | 白毛 中 | 14 | 接地陰影 |
| 7 | 白毛 亮 | 15 | 項圈識別色 |

**規則**：
- 每個材質最多 3 階（暗/中/亮），這決定了 16 色夠不夠用
- 白色不要用純 `#FFFFFF`，用微帶藍灰的白，看起來才有體積
- 描邊色不要用純黑，用材質色的極深版本
- index 15 保留給角色識別色（項圈），四隻狗要明顯不同

`tools/validate.py` 會檢查母版圖的實際色數，超過 16 色直接報錯。

---

## 五、部件切分規格（rig）

> **⚠️ 本節整節是歷史記錄。** 部件階層、pivot、z-order、`cutparts.py` 的切分
> ——這些屬於部件旋轉路線，已於 2026-08-01 移除（實測腳掌會畫弧離開地面，
> 見 docs/05 第三節）。現在的動畫是 AI 一次生成一張含四格的圖再切開，
> 沒有部件的概念。
>
> 留著是因為它記錄了「為什麼只拆七群而不是十六個」那段推理——
> 降採樣的 coverage 會在重疊處把兩邊都吃掉。那個現象和路線無關，將來可能還會遇到。

> ### ⚠️ 真像素藝術與部件旋轉的衝突
>
> 旋轉像素藝術會破壞像素網格，結果會出現不對齊的鋸齒。
> 解法是**在高解析度合成與旋轉，最後才像素化**：
>
> ```
> 部件圖（高解析，AI 原始輸出的解析度）
>   → bake.py 合成 + 旋轉（仍在高解析）
>   → pixelate.py 降到 64px（每一格用同一組格線與同一個調色盤）
>   → spritesheet
> ```
>
> 這樣旋轉發生在像素化之前，最終輸出仍是乾淨對齊的網格。
> 但必須遵守兩條鐵律，否則動畫會抖動或閃爍——見 `docs/05_資產管線.md` 第三節。
>
> **所以部件圖保存在 AI 原始解析度，不要先像素化再切分。**

### 5.1 犬類部件階層

```
root                             (原點 = 地面接觸點)
├── shadow                       接地橢圓陰影
└── body            [pose]       pivot: 髖部中心
    ├── tail                     pivot: 尾根
    ├── leg_hind_far  [pose]     pivot: 髖
    ├── leg_hind_near [pose]     pivot: 髖
    ├── leg_fore_far  [pose]     pivot: 肩
    ├── leg_fore_near [pose]     pivot: 肩
    └── head                     pivot: 頸根
        ├── ear_far              pivot: 耳根
        ├── ear_near             pivot: 耳根
        ├── eye_far              固定
        ├── eyelid_far           pivot: 上眼瞼，靠 y 位移做眨眼
        ├── eye_near             固定
        ├── eyelid_near          pivot: 上眼瞼
        ├── muzzle               固定
        │   ├── jaw              pivot: 下顎關節，靠旋轉開合嘴
        │   └── tongue           顯示/隱藏
        └── (marking_overlay)    僅 mirror 修正用，可選
```

`[pose]` 標記的部件有三套姿態版本：**`stand` / `sit` / `lie`**。
坐姿與趴姿的軀幹與腿部形狀無法由旋轉得到，必須另外畫。

**眨眼用眼瞼位移而非三張眼睛圖**——1 張眼瞼取代 3 張眼睛狀態圖，
而且可以做出任意中間值的半瞇眼。開合嘴同理，用下顎旋轉取代 4 張嘴型。

### 5.2 公主部件階層

```
root
├── shadow
├── hair_back
├── arm_far_upper / arm_far_fore      pivot: 肩 / 肘
├── leg_far           [pose]
├── torso             [pose]          pivot: 髖，服裝為獨立覆蓋層
├── leg_near          [pose]
├── arm_near_upper / arm_near_fore    pivot: 肩 / 肘
│   └── hand_anchor                   掛點（無圖），手持物件用
├── head                              pivot: 頸根
│   ├── hair_front
│   ├── eye_L / eyelid_L
│   ├── eye_R / eyelid_R
│   ├── brow_L / brow_R               pivot: 眉心側
│   ├── mouth                         3 態：閉 / 微笑 / 張
│   ├── blush                         可選層
│   └── crown                         可選層
└── outfit_overlay                    服裝獨立層，套在 torso 上
```

姿態版本：**`stand` / `sit` / `kneel`**（跪下摸狗用）。

### 5.3 部件圖數量估算

| 角色 | 共用頭部群 | 共用 | 三種姿態 | 鏡射需重畫 | 小計 |
|---|---:|---:|---:|---:|---:|
| `chihuahua` | 10 | 2 | 14 | +2 | **28** |
| `brown_mixed` | 10 | 2 | 14 | 0 | **26** |
| `brindle_guard` | 10 | 2 | 14 | +26 | **52** |
| `ice_princess` | 12 | 2 | 18 | +10 | **42** |
| 服裝（5 套 × 3 件） | — | — | — | — | **15** |
| 物件 / UI / 特效 | — | — | — | — | **42** |
| | | | | **合計** | **≈ 205** |

**這 205 張是靜態部件圖，不是動畫影格。**
它們彼此之間不需要影格一致性，每一張都可以獨立慢慢做到好。
做完之後可以生出**無限多**的動畫。

---

## 六、AI 生成批次（實際只需 ~36 張生成）

不逐張生成 205 個部件。改成生成 **部件分解圖（breakdown sheet）**：
一張圖上把所有部件攤開排列在透明背景上，再用 `tools/cutparts.py` 自動切開。

| 批次 | 內容 | 張數 |
|---:|---|---:|
| **B0** | 四角色標準模型表（正面+左右側+背面，含比例尺） | 4 |
| **B1** | 四角色比例對照圖（同一畫面站在一起） | 1 |
| **B2** | 每角色表情表（8 種情緒的頭部特寫） | 4 |
| **B3** | 部件分解圖 — stand 姿 | 4 |
| **B4** | 部件分解圖 — sit 姿 | 4 |
| **B5** | 部件分解圖 — lie / kneel 姿 | 4 |
| **B6** | 鏡射方向補畫（虎斑全套 ×3、吉娃娃頭部、公主頭部） | 5 |
| **B7** | 公主服裝表（5 套） | 1 |
| **B8** | 物件表（食物 / 玩具 / 家具 / 特效 / UI 圖示） | 5 |
| **B9** | 場景背景（臥室 / 用餐 / 遊戲 / 夜晚） | 4 |
| | **合計** | **36** |

計入重生與挑選的損耗（約 2×），實際生成次數約 **70 次**。

**批次之間必須循序完成，不可跳批。**
B0 未通過驗收前不做 B1，B1 未定案前不做 B2 以後的任何一批。
這就是原始規劃裡「先做四角色標準模型表」的正確執行方式。

---

## 七、動畫清單

動畫定義在 `specs/animations/<char>.anim.json`，**是純資料，不是圖檔**。
新增一個動畫的成本是寫 20 行 JSON，不是生 6 張圖。

### 7.1 每隻狗（21 個動畫）

| id | 循環 | 影格 | fps | 主要運動部件 |
|---|:---:|---:|---:|---|
| `idle_breathe` | ✓ | 8 | 6 | body y 微幅、head 微幅 |
| `idle_blink` | | 4 | 8 | eyelid y |
| `idle_look` | | 12 | 6 | head rot、eye 偏移 |
| `idle_ear_twitch` | | 6 | 8 | ear rot |
| `tail_wag` | ✓ | 6 | 10 | tail rot |
| `sit_down` | | 8 | 8 | pose stand→sit |
| `stand_up` | | 8 | 8 | pose sit→stand |
| `lie_down` | | 10 | 8 | pose sit→lie |
| `walk` | ✓ | 8 | 10 | 四腿 rot、body 上下 |
| `turn` | | 6 | 10 | 整體翻轉過渡 |
| `eat` | ✓ | 6 | 8 | head 俯下、jaw 開合 |
| `eat_happy` | | 10 | 6 | tail、head、jaw |
| `sleep_breathe` | ✓ | 16 | 4 | body 極緩、eyelid 全閉 |
| `yawn` | | 12 | 6 | jaw 大開、eyelid |
| `wake_up` | | 10 | 6 | pose lie→sit、eyelid |
| `play_bow` | | 8 | 8 | 前腿下伏、臀部上抬 |
| `chase_ball` | ✓ | 8 | 12 | 快速四腿循環 |
| `happy` | | 12 | 8 | 全身彈跳、tail 快搖 |
| `sad_wait` | ✓ | 12 | 4 | ear 下垂、head 低、動作慢 |
| `pet_react` | ✓ | 8 | 8 | head 蹭、eyelid 半閉 |
| `toilet` | | 14 | 6 | pose 過渡、姿勢暗示 |

`toilet` **只用姿勢與表情暗示，不畫任何排泄物**。

### 7.2 冰雪公主（21 個動畫）

> **⚠️ 本節作廢。** 原本規劃她有自己的一套動畫（`kneel_down`、`pet_dog_small`、
> `throw_ball`、`dress_change`、`sit_chair`、`jump_trampoline`、`greeting`），
> 每個 8-16 格。

實際做法是**沿用和狗完全相同的 21 個 `anim_id_t`**，理由是韌體：
`firmware/include/game.h` 只有一個 enum，`game.c` 的 `game_is_dog()` 只分流
**需求**（`NEED_BLADDER` 給狗、`NEED_TIDY` 給公主）不分流閒置池。
所以她一定會播到 `tail_wag` 與 `idle_ear_twitch`——那些必須有人形的對應動作。

重新詮釋的五個（見 `specs/anim_prompt/ice_princess.json` 的 `FRAMES_OVERRIDE`）：

| `anim_id_t` | 狗 | 公主 |
|---|---|---|
| `idle_ear_twitch` | 耳朵抽動 | 撥開臉頰的髮絲 |
| `tail_wag` | 搖尾 | 裙襬與圍巾輕輕搖晃 |
| `play_bow` | 邀玩鞠躬 | 屈膝禮 |
| `chase_ball` | 追球 | 把球丟出去給狗撿 |
| `toilet` | 上廁所 | 蹲下和狗平視（實際上永遠不會被播到） |

影格數與其他角色一致：21 個動畫、83 格。

---

### 7.3 群組動畫（6 個）

`all_idle`、`all_eat`、`all_sleep`、`dogs_follow_princess`、
`dogs_sit_in_line`、`family_group_pose`

群組動畫**不是新的圖**，是多個角色 rig 在同一時間軸上的編排，
定義在 `specs/animations/group.anim.json`。

### 7.4 總量與 Flash 驗算

```
動畫總數    21 × 3 + 21 + 6 = 90
平均影格數  9
烘焙影格    90 × 9 ≈ 810

單格（solo 最大 112×88）:
  4bpp 索引        112 × 88 ÷ 2 = 4,928 B
  RLE 後（實測約 2.5×）  ≈ 1,970 B

810 × 1,970 ≈ 1.60 MB
加上 group 縮放的常用動畫（+15%）  ≈ 1.84 MB
加上物件 / UI / 場景             ≈ 2.4 MB
```

`assets` 分區 **6 MB** → 餘裕 **2.5×**。✅

對照：原規劃的 250 張 128×128 RGB565 未壓縮 = **7.81 MB**，已經超過可用空間。
部件路線在動畫數量多 3 倍的情況下，佔用只有原方案的 **31%**。

> **備援方案**：若日後動畫量再膨脹到塞不下，可改為裝置端執行期合成
> （只存 205 個部件圖，約 0.4 MB）。代價是 CPU 開銷。
> 目前 flash 餘裕充足，優先選擇烘焙好的完整影格，換取執行期零成本。

---

## 八、檔案命名

### 部件圖（`art/rigs/<char>/`）

```
<character>_<pose>_<part>_<dir>.png
```
```
chihuahua_stand_body_r.png
chihuahua_stand_leg_fore_near_r.png
chihuahua_head_ear_near_r.png
chihuahua_head_eyelid_far_l.png       ← 鏡射不安全，另畫
brindle_guard_sit_body_l.png
ice_princess_kneel_torso_r.png
```

- `<pose>`：`stand` / `sit` / `lie` / `kneel` / `head`（頭部群不分姿態）
- `<dir>`：`r`（面向右，基準方向）/ `l`（面向左，僅 `mirror_safe: false` 的部件需要）

### 烘焙產物（`build/sheets/`）

```
<character>_<anim>_<dir>_<profile>.png      spritesheet
<character>_<anim>_<dir>_<profile>.json     atlas（每格位置、anchor、時長）
```

### 生成原圖（`art/generated/`）

```
<batch>_<character>_<shot>_<序號>.png
B3_chihuahua_breakdown_stand_03.png
```

保留所有生成結果（含被退回的），方便回溯與比對。

---

## 九、生成提示詞規範

每次生成的提示詞由 **四個區塊**組成，由 `tools/genprompt.py` 自動拼接：

```
[1] STYLE LOCK      ← specs/style_lock.md，逐字不變
[2] CHARACTER       ← specs/characters/<char>.json 的 identity_features
[3] SHOT            ← 本次要的視角 / 姿態 / 版面
[4] CONSTRAINTS     ← 負面約束（無背景、無漸層、無文字浮水印…）
```

**強制規則**：

1. **[1] STYLE LOCK 一字不改。** 這是跨角色風格一致的唯一保證。
   改了它，四個角色就不會像是同一個世界的。
2. **B1 之後的每一次生成都必須附上已通過的參考圖**作為影像輸入
   （`art/approved/` 裡的模型表）。純文字生成無法維持角色一致性。
3. 一次只生成一個角色。**不要在同一張圖裡生兩隻狗**（B1 比例對照圖除外）。
4. 部件分解圖要在提示詞裡明確要求「各部件之間留白分開、不重疊、透明背景」。

---

## 十、QA 驗收清單

每一張生成圖進 `art/approved/` 前，逐項確認。**任一項不過就退回重生。**

### 通用

- [ ] 背景完全透明，無殘留白邊或棋盤格
- [ ] 無漸層、無柔邊、無發光、無模糊
- [ ] 描邊完整封閉，粗細一致
- [ ] 實際色數 ≤ 16（跑 `python tools/validate.py --image <path>`）
- [ ] 無文字、無浮水印、無簽名
- [ ] 視角為 3/4 側面，與前一批一致
- [ ] 光源方向與前一批一致（預設左上 45°）

### 比例

- [ ] 角色全高符合第二節的比例鎖定表（±3%）
- [ ] 頭身比正確
- [ ] 眼睛佔頭寬比例正確
- [ ] 與同批其他角色並排時，相對大小正確

### 身份特徵（逐角色對照第三節）

- [ ] 三節裡該角色的每一項 `identity_features` 都出現且正確
- [ ] 毛色與參考照片一致（並排比對，不憑印象）
- [ ] **不對稱特徵在正確的一側**
- [ ] 虎斑護衛犬：未露牙、未皺鼻、無威嚇姿態

### 部件分解圖額外檢查

- [ ] 所有必要部件都在，沒有漏
- [ ] 部件之間有留白，沒有重疊或相黏
- [ ] 同一部件在不同姿態圖裡的**顏色完全一致**
- [ ] 關節處有足夠的重疊餘量（旋轉時不會露出縫隙），建議 8 px

---

## 十一、實際製作順序

原本的規劃（部件路線）：

```
0. 收齊照片 → 填 TBD → 1. specs/ → 2. B0 模型表 → 3. B1 比例對照圖
→ 4. 調色盤 → 5. B2 表情表 → 6. B3-B5 部件分解圖 → 7. B6 鏡射補畫
→ 8. cutparts.py 切分 → 9. 第一個動畫 → 10. B7-B9 物件場景 → 11. bake + pack
```

**實際做出來的順序**（四個角色都跑過一次，見 CLAUDE.md 的「做一個新角色」）：

```
1. specs/characters/<id>.json  身份特徵 + render.sprite_cell
2. specs/palettes/<id>.json    16 色手工制定，色相／明度避開已有角色
3. specs/anim_prompt/<id>.json 角色填充（SUBJECT / IDENTITY / 節奏 / 覆寫）
4. mkprompt.py --all           組出 16 份提示詞
5. master 一張 → pixelate 掃寬度 → 色群 → remap → pixedit → REBUILD.sh
6. 15 張影格圖 → procanim.sh ×15
7. specs/animations/<id>.anim.json → bake.py → preview.py
8. validate.py --rebuild       逐位元可重現才算完成
```

B1 比例對照圖、B2 表情表、B3-B6 部件分解圖、B7-B9 物件場景**都沒有做**——
前面幾項是部件路線的中間產物，後面幾項還沒排到。
四角色的比例對照是直接量成品 sprite 出來的（見 docs/09 附錄）。

**步驟 3 的四角色比例對照圖是整個專案最重要的一張圖。**
它一旦定案，後面兩百多張圖才有共同的基準。在它通過驗收之前，
不要因為想快一點就先做別的批次。
