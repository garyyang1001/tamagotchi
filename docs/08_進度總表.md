# 08 — 進度總表

> 本文件記錄目前狀態與**實測驗證過的結論**。
> 每一條結論後面都附了實際數據，因為它們多數推翻了原本的規劃。

---

## 一、整體進度

```
██████████████████░░░░░░░░░░░░  規格與文件        92%
████████████████████████████░░  資產管線工具      95%   兩階段烘焙全線打通
██████████░░░░░░░░░░░░░░░░░░░░  角色資產          35%   1/4 角色 21 個動畫可播，但畫質有已知缺陷
██████████░░░░░░░░░░░░░░░░░░░░  韌體              33%   存檔 + 遊戲邏輯完成
░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  硬體              0%    尚未採購
```

### 已完成且經測試

| 項目 | 證據 |
|---|---|
| 存檔層 `save.c` | 主機端測試 0 失敗，含掉電中斷後舊存檔完好 |
| 遊戲邏輯 `game.c` | 主機端測試 0 失敗，含「放置一個月」的無懲罰驗證 |
| `pixelate.py` | 27,990 色的 AI 產出 → 9 色乾淨像素圖 |
| `cutparts.py` | 16/16 部件正確切出，洋紅殘留 0 px |
| `pixedit.py` | 手工畫出 3×3 的眼睛（虹膜 + 高光） |
| `validate.py` | `brown_mixed` 通過契約，可重現性逐位元相同 |
| `assemble.py`（階段 A） | 自我測試 12/12；16 部件 → 7 群 × 3 姿勢 = 21 張 1344×1176 |
| `bake.py`（階段 B） | 自我測試 25/25；21 個動畫 85 格，跑兩次逐位元相同 |
| `preview.py` | 自我測試 22/22；21 個 GIF + 接觸表 + 320×240 實機模擬 |
| `brown_mixed` 動畫 | 21/21 全部 `IMPLEMENTED`，85 格（預算 86），0 錯誤 |

### 未開始

| 項目 | 卡在哪 |
|---|---|
| **階段 A 的畫質** | 三個姿勢的角色只有 43×41 px，master 是 64×54 —— 見 2.7，**目前唯一的關鍵路徑** |
| `pack.py`（RLE + 資產包） | 可以開始，spritesheet 已經有了 |
| `chihuahua` / `brindle_guard` | **缺參考照片** |
| `ice_princess` | 需設計決策（髮辮側、服裝） |
| 渲染層、觸控、音效、電源管理 | 等資產包格式定案 |
| 硬體採購、外殼 | 未開始 |

---

## 二、已驗證的結論

這些是實測出來的，不是推論。它們推翻了原始規劃的多個假設。

### 2.1 影像模型的能力邊界

| 能做 | 不能做 |
|---|---|
| 造型設計、體型比例 | 產出乾淨的像素網格 |
| 大面積配色、標記位置 | 打準指定顏色 |
| 剪影與辨識特徵 | 控制 5 像素以下的特徵 |
| 部件分解圖（部件是獨立大圖） | 維持跨姿勢的角色一致性 |

**實測數據：**

```
宣稱「乾淨的像素網格、10 色」
實得  27,990 色、色塊長度 2/3/4/5 混雜、
      「純洋紅」背景是 #F703F5 / #F803F5 / #F702F5… 十幾種雜訊值

要求「眼睛放大 20%，虹膜與高光都要讀得出來」
實得  3×2 像素的純暗色塊，沒有虹膜也沒有高光

要求「去飽和的灰白吻部，almost silver-grey」
實得  飽和度 80 的暖褐色（目標是 26）

指定  1024×1024
實得  1341×1173
```

**對策**：全部在下游修，不要重生。`--remap` 把飽和度 80 修成 26，
`pixedit.py` 手工畫眼睛。分工是固定的，見 `CLAUDE.md` 規則 2。

### 2.2 換姿勢會造成身分漂移

以已核可的 master 為參考圖，明確要求「這是同一隻角色的不同姿勢，
不要重新設計、不要變得更可愛更年輕」，生成三個姿態：

| 姿態 | 結果 |
|---|---|
| 坐姿 | 變圓變幼，灰白吻部退回暖褐 |
| 趴姿 | 同上 |
| 正面 | 徹底的重新設計，眼睛變成褐色圓環 |

三張朝**同一個方向**漂移，這是系統性的。

**對策**：只生站姿的部件分解圖，其餘姿態由部件推導。
附帶收益：部件分解圖從三張降為一張。

### 2.3 拆開來畫，模型就做得到

同一個模型，在**組裝好的圖**上畫不出 3 像素的眼睛，
但在**部件分解圖**上（眼睛是獨立的大圖）就正確畫出了琥珀虹膜與白色高光。

這解釋了為什麼部件路線不只是為了動畫——它也提升了美術品質的上限。

### 2.4 語意重映射不能用數值距離

把手工調色盤用「最近 RGB 距離」套到 AI 產出上：
狗身上的中棕色像素因為數值上離 `muzzle_grey_dark` 比離 `coat_mid` 更近，
**整片肚子和腿被吸成灰白色**。

而且同一個來源色在不同部位有不同語意——AI 在吻部和小腿用了同一個 `#D0A980`，
整張圖一起 remap 會讓腳跟著吻部一起變灰。

**對策**：`--remap` 用人工填寫的語意對照表，並支援 `bbox` 區域限定。

### 2.5 Tier 0 的真實範圍比想像小很多

原本宣稱 21 個動畫都能用整隻 sprite 的變換做出來。錯了。

| 錯誤 | 實際效果 |
|---|---|
| `sit_down` 用 `scale_y: 86%` | 讀起來是狗被壓扁，不是狗坐下 |
| `eat` / `pet_react` 用 `rot: 4°` | 64px 上旋轉產生歪斜鋸齒，破壞像素網格 |
| `turn` 用 `scale_x: 40%` | 狗被橫向壓扁，不是轉身 |

**坐姿是不同的剪影，不是同一個剪影變矮。**

重新劃線後只有 **5 個**動畫符合 Tier 0：
`idle_breathe` `walk` `turn` `happy` `sad_wait`。

Tier 0 現在只允許**整數像素位移與水平翻轉**。

### 2.6 測試抓到的、在板子上很難發現的 bug

| Bug | 症狀 |
|---|---|
| `now_unix += dt_ms / 1000` | 100ms 步長時整數除法恆為 0，時鐘完全停住，夜間模式永不觸發 |
| 里程碑 `1 << m` | bit 0 是預設服裝，第一個里程碑等於什麼都沒給 |
| `apply_decay` 用函式靜態計數器 | 多個 game 實例互相污染 |
| `sad_wait` 機率 `50 + (40-low)` | 需求全零時 90% 時間垂頭喪氣，對 3 歲太沉重（已壓到 57%） |
| `cutparts` 投影分割 | 兩個部件投影重疊，16 個只切出 15 個 |
| `cutparts` 列分組用 `y0 // 門檻` | 矮部件（尾巴）上緣位置低，被誤分到下一列 |
| `cutparts` 用外接框裁切 | 帶走鄰近部件探進框內的像素 |
| 去背只靠色度門檻 | 邊緣殘留 562 px 洋紅混色（已加去溢色，歸零） |

### 2.7 端到端跑完之後才看得到的四件事

21 個動畫全部烘出來、放在同一張接觸表上比對之後，浮出四個之前看不到的問題。
全部有數字，全部記在 `build/preview/_REPORT.md`。

**(1) 階段 A 的角色比 master 小了一半 —— 這是最嚴重的一件事**

| 來源 | 尺寸 | 不透明像素 |
|---|---|---:|
| master sprite（5 個 transform 動畫用） | 64 × 54 | 1,809 |
| 階段 A 的 stand（16 個 rig 動畫用） | 43 × 41 | 914 |

同一隻狗在 `idle_breathe`（master）與 `idle_blink`（rig）之間切換時會**當場縮水
三分之一**。原因是部件在 `art/rigs/` 的原生解析度偏小，擺進 1344×1176 只佔到
約 903×861。修法是階段 A 把部件整體放大約 1.5 倍（階段 A 允許重新採樣），
或反過來把 master 重做成與部件同尺度。

同一個根因也吃掉了辨識特徵：rig 姿勢缺 `eye_dark`、`eye_highlight`、`tan_dark`
三個色階，**眼睛沒有瞳孔也沒有高光**，臉在 1× 下讀不出來。

**(2) lie 姿勢的脖子有 0 px 餘裕**

實測掃描 head 圖層在 lie 姿勢的所有 `dx`（−3..+3）× `dy`（−4..+4）組合：
只要 `dy ≤ −1`，整顆頭就與身體斷成兩個連通區域，`dx` 完全不影響。
`lie_down` f2 與 `wake_up` f1 原本各抬頭 2 px，畫面上是一顆浮空的頭。
已改成 `dy = 0`，代價是「頭尚未落定」那一拍沒了。

lie 姿勢另外還有一個 6 px 寬的洞（第 46–51 列的 x35–40），前腿與胸口分離，
高解析原圖就是分離的。

**(3) 尾巴往左抬就會脫離身體**

`tail` 的 z 最低（10），尾根靠鑽進軀幹裡藏起來。`tail_wag` 的作者量過這件事並
寫進 `_note`，但 `play_bow` f2（−1,−2）與 `chase_ball` f2（−2,−2）都往左移，
兩格各產生一條 35 px 的浮空尾巴。已分別改成 (+1,−2) 與 (0,−2)。

**(4) 64×56 的畫布沒有上下餘裕**

master 是 64×54、底部對齊，頭上只剩 2 列。`happy` 原本跳 7 px，
第 1–3 格共 141 px 被切掉，切的正好是頭頂與耳尖。已把幅度砍到 2 px——
**不會再切到頭，但 2 px 的彈跳表達不出「開心」，這是目前最弱的一支動畫。**
`sad_wait` 是同一個根因的下緣版本，四格共 38 px 的腳掌被切掉（未修）。

> 三個 `--check` 也順手證實了一件好事：三個姿勢**確實是不同的剪影**，
> 不是同一個剪影變矮。stand↔sit 的 IoU 0.643、stand↔lie 0.295、sit↔lie 0.356。

---

## 三、關鍵數字

| 項目 | 值 |
|---|---|
| master sprite 尺寸 | 64 × 54，1,809 個不透明像素 |
| 影格畫布 | 64 × 56（= 1344/21 × 1176/21） |
| 階段 A 姿勢尺寸 | stand 43×41 / sit 45×39 / lie 54×26 ← **偏小，見 2.7** |
| 調色盤 | 16 色 4bpp（master 用 10 色，rig 影格 8–9 色） |
| 部件數 | 高解析 16 → 像素領域 7 個圖層群 |
| 動畫數 | 21，共 85 格（預算 86） |
| 四角色同框寬 | 254 px（< 320，所以不需要兩套縮放） |
| **RLE 實測** | 4bpp 原始 1,792 B/格 → RLE 615 B/格，**2.91×**（原估 2.5×） |
| `brown_mixed` 全部動畫 | 85 格 × 615 B ≈ **51 KB** |
| 四角色外推 | ≈ 205 KB，加物件／UI／場景／服裝 ≈ 400 KB |
| `save_blob_t` | 128 bytes |
| 存檔壽命 | 約 9 年（雙槽交替、300 秒間隔） |

`assets` 分區 6 MB → 佔用 **約 6.7%**，餘裕 15 倍。

對照原始規劃：250 張 128×128 RGB565 未壓縮 = **7.81 MB**，
已超過整片 flash 扣掉韌體後的可用空間。

---

## 四、待辦與依賴

```
#17 階段 A 重做：部件放大到與 master 同尺度  ← 新的唯一關鍵路徑
     └→ 重跑 assemble → pixelate → bake → preview（全自動，改完就有）
          └→ #18 補回 eye_dark / eye_highlight（pixedit.py 逐像素）
               └→ pack.py → 資產包 → 渲染層

#19 lie 姿勢修前腿與脖子（specs/poses 的 x/y，必須是 21 的倍數）
#20 happy 的跳躍改成遊戲層繪製偏移，把 7px 幅度拿回來
#4  B2 表情表                      獨立，隨時可做
#9  歸檔與完成報告                 等 #17

新角色（chihuahua / brindle_guard）  ← 缺參考照片
ice_princess                        ← 需設計決策
硬體採購                            ← 獨立，隨時可做
```

### 現在需要使用者提供的

1. **吉娃娃與虎斑護衛犬的照片** —— 四角色比例對照圖需要四隻都定義完才畫得出來
2. **`brown_mixed` 的側面站姿照片** —— 目前用 v4 當側面基準，是推導不是實據
3. **公主的設計決策** —— 髮辮在哪一側（決定哪些部件不能鏡射）
4. **硬體採購** —— 開發板型號確定後回填 `docs/01`

---

## 五、尚未驗證的假設

誠實記錄還沒被測試打過的東西：

- **3 歲測試（真人）** —— 全部驗收到目前為止都是在螢幕上放大 6 倍看的。
  1× 縮放、手臂距離、只看 1 秒的測試**一次都還沒做**。以 2.7 的畫質問題來看，
  這關很可能過不了。
- **`rig.json` 的 pivot 是否正確** —— 姿勢看起來成立，但 pivot 的絕對正確性
  是靠 `specs/poses` 的座標補償出來的，不是驗證過的。
- **`offset` 全為 0** —— 部件相對父階層的位置還沒填，姿勢座標是絕對值。
- **ESP32 上的實際幀率與功耗** —— 沒有硬體，全是紙上計算。
- **`frame_ms` 在韌體端是否被尊重** —— 動畫定義用不等長影格（如 `yawn` 的
  460ms 停頓、`sit_down` 的 250/125/125/375），渲染層還沒寫，可能只支援固定 fps。

### 已經不再是假設的（本輪驗證掉的）

- ~~部件合成後再像素化品質是否可接受~~ —— 可接受，但**尺度不對**，見 2.7(1)。
  管線本身（21:1 決定性映射、階段 B 零重採樣）是成立的：`bake.py` 的
  「七層零位移合成 = 原姿勢圖，1809 px 完全復原」與「跑兩次逐位元相同」都通過。
- ~~RLE 壓縮率 2.5×~~ —— 實測 **2.91×**，615 B/格。比原估好。
